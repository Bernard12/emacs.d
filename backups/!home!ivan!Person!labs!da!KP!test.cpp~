#include <iostream>
#include <map>
#include <vector>
#include <functional>

int main(){
    std::vector<std::function<void()>> b;
    std::map<int,int> a;
    auto t  = [&](){
        std::cout << "Hel/company-mode.el
    ~/.emacs.d/init.el
    ~/.emacs.d/rc/irony.ello world";
    };
    b.push_back(t);
    return 5;
}
